version: "3.9"

services:
  backend:
    build:
      context: .
    container_name: backend_image
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: /bin/sh -c "load_data && start"
